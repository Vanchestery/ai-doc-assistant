# Скопируй в .env рядом с docker-compose.yml (файл .env в git не попадает)
DEEPSEEK_API_KEY=sk-your-key-here

# Опционально: свой пароль БД для prod-окружения
# POSTGRES_PASSWORD=change-me-in-production
